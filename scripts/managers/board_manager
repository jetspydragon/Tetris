export class BoardManager
{
    constructor(private board:number[][])
    {
        for(let x = 0; x < 10; x++)
        {
            for(let y = 0; y < 20; y++)
            {
                board[x][y] = 0;
            }
        }
    }

    
}